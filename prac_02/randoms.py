# 1. 5 is the smallest possible number and 20 is the largest.
# 2. No line 2 could not of produced a 4 because it starts at 3 and only goes up in 2's so 3, 5, 7, so on. meaning the largest number would have to be 9.
# 3. 2.5 is the smallest number possible and 5.5 is the largest.
import random
print(random.randint(1, 100))